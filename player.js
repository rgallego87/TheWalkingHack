function Player(position) {
  this.currentRow   = position.x,
  this.currentCol   = position.y,

  this.playerChar   = position.playerChar  // Control character for player
}

Player.prototype._renderPlayer = function() {
  
}